import ToolsPage from '@/components/ToolsCompents'
import React from 'react'

export const metadata = {
  title: "All Tools",
  description: "Browse 15+ free tools for image, audio, video, and text — filter by category or search.",
};

const page = () => {
  return (
    <ToolsPage/>
  )
}

export default page